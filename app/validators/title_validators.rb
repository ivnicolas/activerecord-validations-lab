class TitleValidator < ActiveModel::Validator
    # def validate (record)
    #     unless record.title.include("Wont Believe" || "Secret" || "Guess" || "Top")
    #         record.errors[:title] << "This post is no click-baity!"
    #     end 
    # end 
end 